Setting up the chatbot on local:

- Install the module
- Create the chatbot in the content
- Publish the website
- Configure the http/https mapping in IIS for the website (generate the https certificate using the tool)
- Go to the chatbot item and configure the access token for the chatbot
- Run an NGROK and configure the hostname for endpoint in the chatbot module settings